#include <bits/stdc++.h>

using namespace std;

int main()
{
    // The Following Lines Makes I/O More Efficient
    ios::sync_with_stdio(false);
    cin.tie(0);

    // freopen("input.txt", "r", stdin);
    // freopen("output.txt", "w", stdout);

    // getline(cin,string_obj)
    /* 
    amount of data is unknown
    while(cin >> x) {
        
    }



 */

    return 0;
}