#include <bits/stdc++.h>
typedef long long ll;
using namespace std;
#define F first
#define S second
#define PB push_back
#define MP make_pair
#define REP(i, a, b) for (int i = a; i <= b; i++)
#define SQ(a) (a) * (a)

int main()
{
    double x = 0.3 * 3 + 0.1;
    printf("%.20f\n", x); // 0.99999999999999988898 rounding error

    double a;
    double b;
    if (abs(a - b) < 1e-9)
    {
        // numbers are equal
    }

    return 0;
}